ext {
  agentTargetDir = file('target')
}

dependencies {
  compileOnly fileTree(dir: 'lib', include: '*.jar')
}

jar {
  archiveBaseName = 'remora-agent'
  destinationDirectory = agentTargetDir
}

clean {
  delete agentTargetDir
}
